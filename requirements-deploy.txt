# ── Core framework ──
fastapi==0.115.6
uvicorn==0.34.0
python-multipart

# ── Computer-vision (CPU) ──
opencv-python-headless==4.10.0.84
numpy==1.26.4
scipy==1.13.0
pillow==11.0.0
matplotlib==3.9.0
pandas==2.2.2
deepface==0.0.93
mediapipe==0.10.14

# ── Text & audio ML (CPU-only torch – installed via --extra-index-url) ──
transformers==4.46.3
torch==2.5.1+cpu
soundfile==0.12.1
audioread==3.0.1
scikit-learn==1.5.2

# ── TensorFlow CPU ──
tensorflow-cpu==2.18.0
tf-keras>=2.18.0
keras==3.8.0

# ── Whisper / HuggingFace helpers ──
huggingface-hub>=0.26.0
safetensors>=0.4.5

# ── Groq AI ──
groq>=0.12.0

# ── OAuth / HTTP ──
httpx>=0.27.0
itsdangerous>=2.2.0

# ── Auth (JWT, hashing, email validation) ──
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
email-validator>=2.0.0

# ── Async email (feedback/crisis escalation) ──
aiosmtplib>=3.0.0

# ── Rate limiting ──
slowapi>=0.1.9

# ── Database (async Postgres) ──
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29.0
alembic>=1.13.0

# ── Misc ──
python-dotenv>=1.0.0
setuptools
